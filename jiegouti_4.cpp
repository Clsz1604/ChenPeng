//
// Created by Admin on 2021/9/25. 通过函数完成对结构体的输入输出
//
# include <stdio.h>
# include <string.h>

/*
struct student
{
    int age;
    char name[100];
    char sex;
};

void InPutStudent(struct student *pstu);
void OutPutStudent (struct student ss);

int main13(void)
{
    struct student st;
    printf("%d\n",sizeof(st));

    InPutStudent (&st);

    //printf("%d %s %c\n", st.age, st.name, st.sex);
    OutPutStudent (st);


    return 0;
}

void OutPutStudent (struct student ss)
{
    printf("%d %s %c\n",ss.age, ss.name ,ss.sex);
}

void InPutStudent(struct student *pstu)
{
    (*pstu).age = 10;
    strcpy(pstu->name, "张三");  //不能写成sut.age="张三"
    pstu->sex = 'F';
}
*/